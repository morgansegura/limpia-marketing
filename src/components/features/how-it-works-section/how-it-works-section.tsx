import { cn } from "@/lib/utils";

import "./how-it-works-section.css";

type THowItWorksSectionProps = {
  className?: string;
};

export function HowItWorksSection({ className }: THowItWorksSectionProps) {
  return (
    <div className={cn("how-it-works-section", className)}>
      {/* TODO: Implement how-it-works-section */}
    </div>
  );
}
